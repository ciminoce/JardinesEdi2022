@model IEnumerable<JardinesEdi2022.Entidades.Entidades.Carrito>

@{
    ViewBag.Title = "MostrarCarrito";
}

<h2>MostrarCarrito</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClienteId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClienteId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CarritoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CarritoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CarritoId })
        </td>
    </tr>
}

</table>
